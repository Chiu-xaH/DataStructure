广义表：广义表(Lists，又称列表)是一种非线性的数据结构，是线性表的一种推广。二维以上的数组其实是一种特殊的广义表。

深度：表展开后含括号的层数

tail 函数去掉第一个元素，返回剩下的部分
head函数返回第一个元素

对于 非空广义表 来说，它的表头（即Head）既可能是单元素又可能是广义表。
例如：
Head（ （a,b,c） ）= a ;
Head（（（a）,（b）））=（a）;
但其表尾（即Tail）一定是一个广义表，也就是说，广义表利用Tail提取后也一定还是一个广义表。这么说有点像废话，其实不然，这里面就隐藏着一个初学者很容易踩到的陷阱。
