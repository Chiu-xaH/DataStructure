1.构建哈夫曼树（最优二叉树）
将数据从小到大排序{3,4,5,6,8,10,12,18}
然后选取最小的两个元素，其中最小的作为左孩子，另一个作为右孩子，根结点是两数之和
将根结点替代原两个元素放入排序列表，再次选出最小的两个数，再重复上面操作，直到完全构造出树
2.计算WPL
构建哈夫曼树之后，将原元素（一定是叶子）圈出来，数从树根到每个元素需要经过几条腿，则为路径长度
求和（元素*路径长度）的结果为WPL（带权路径长度）
3.哈夫曼编码
按上述构建好哈夫曼树后，每个左腿0，每个右腿1，将原元素圈出来，数从树根到每个元素需要依次经过腿的01，记录下来的就是编码
编码后的文件长度即为WPL